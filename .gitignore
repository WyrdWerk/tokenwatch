# dependencies
node_modules/

# misc
.DS_Store
*.log

# IDE / tool metadata
.reasonix/
.vscode/
.idea/

# secrets — never commit credentials to a public repo
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Cloudflare wrangler local state
.wrangler/
# Local-only copy of wrangler.d1.toml for D1 price-history dev (see
# migrations/README.md). MUST NOT be committed or deployed — Wrangler
# auto-discovers wrangler.toml and would bind the placeholder PRICE_HISTORY
# database to production on `wrangler pages deploy`.
wrangler.toml
.dev.vars
.empryo
.opencode-trace/

# Agent state
.pi/

# Investigation artifacts (one-off analysis dumps, not source)
models_dev_api.json
final_output.json
final_report.md
investigation_summary.md
pipeline-investigation.md
tmp/

# public/h/ is DEPLOYED but NOT committed — do NOT ignore it!
# (wrangler skips gitignored files; ignoring /h/ caused the 2026-07-30
#  broken-SPA-fallback outage. bust-cache.mjs generates these at deploy.)

# Neuralwatt estimator cache (runtime artifact, not source)
data/neuralwatt-estimator-cache.json

# Local-only notes (never publish)
.local/

# Amp orb generated portal manifests (keep .amp/services.yaml tracked)
.amp/portals/

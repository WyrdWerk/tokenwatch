{
  "name": "tokenwatch",
  "version": "1.0.0",
  "description": "TokenWatch — compare pay-as-you-go LLM inference pricing across providers",
  "scripts": {
    "fetch": "node scripts/fetch-pricing.mjs",
    "serve": "npx --yes serve public -l 3000",
    "build:prod": "node scripts/fetch-pricing.mjs && echo 'Static site in ./public — deploy to GitHub Pages or any static host'"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
